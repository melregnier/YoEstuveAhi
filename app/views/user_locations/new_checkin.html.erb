<div class="container p-3 my-3 border">
    <h1>Carga el código QR de la locación</h1>
</div>
<div class="container p-3 my-3 border">
    <%= form_for(@user_location)  do |f| %>
    <%= f.file_field :qr_image, required: true, class:"form-control" %>
    <br>
    <%= f.submit 'Ingresar', class: "btn btn-outline-success"%>
    <% end %>
</div>
<% if current_user.at_risk? %>
<div class="alert alert-warning" role="alert">
    <h4> Usted se encuentra en riesgo. Intente ingresar lo menos posible a locaciones para evitar futuros contagios.
        En caso de poseer un test negativo, puede ingresarlo
        <%= link_to "aquí", '/tests/negative', class: 'text-warning'%>. </h4>
</div>
<% end %>